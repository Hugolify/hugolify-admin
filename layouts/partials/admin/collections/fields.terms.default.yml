{{- $fields := slice 
  "id" 
  "is_not_index" 
  "draft" 
  "title_page" 
  "description"
  "slug" -}}
{{- range $fields -}}
  {{- $file := print "admin/fields/" . ".yml" -}}
  {{- if templates.Exists ( printf "partials/%s" $file ) -}}
    {{ partial $file }},
  {{- end -}}
{{- end -}}