{{- $author := dict "relation" (dict "collection" "persons" "name" "authors" "multiple" true) -}}
{{- $categories := dict "relation" (dict "collection" "categories") -}}
{{- $tags := dict "relation" (dict "collection" "tags" "multiple" true) -}}
{{- return slice 
  "id" 
  "is_not_index" 
  "draft" 
  "title_page" 
  "description"
  "featured_image" 
  $author
  $categories
  $tags
  "body"
  "slug" -}}
