
collections: [
  {{ range site.Params.admin.collections }}
    {{ $file := print "admin/collections/" . ".yml" }}
    {{ if templates.Exists ( printf "partials/%s" $file ) }}
      {{ partialCached $file . }},
    {{ end }}
  {{ end }}
]