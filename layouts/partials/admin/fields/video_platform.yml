
{{- $options := slice 
  "dailymotion" 
  "twitch" 
  "vimeo" 
  "youtube" }}
{{- $options = partial "admin/func/GetOptions.html" (dict "field" . "options" $options) -}}

{{- $args := dict 
  "label" (i18n "admin.fields.video.platform.label")
  "hint" (i18n "admin.fields.video.platform.hint")
  "name" "platform"
  "required" true
  "options" $options
  -}}

{{ partial "admin/widgets/select.js" $args }}
