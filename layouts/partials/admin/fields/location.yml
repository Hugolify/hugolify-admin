{{- $args := dict 
  "label" (i18n "admin.fields.location.label")
  "name" "location"
  -}}

{{- with site.Params.admin.widgets.map.default }}
  {{ $args = $args | append (dict "default" .) }}
{{- end }}

{{ partial "admin/widgets/map.js" $args }}
