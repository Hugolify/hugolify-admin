{{- $name := dict "name" "text" }}
{{- if or (eq site.Params.admin.cms "tinacms") (eq site.Params.admin.cms "cloudcannon") }}
  {{- $name = dict "name" "text_markdown" "nameOverride" "text" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.text.label")
  "minimal" true
  "buttons" "['bold', 'italic', 'link', 'bulleted-list']"
  "modes" "['rich_text']"
  "editor_components" "[]"
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/markdown.js" $args }}
