{{- $fields := slice 
  "title" 
  "url" 
  "is_blank" 
  "is_highlighted" 
  "sublinks" -}}
{{- $fields = partial "admin/func/GetFields.html" (dict "field" . "fields" $fields) -}}

{{- $args := dict 
  "label" (i18n "admin.fields.links.label" 2)
  "label_singular" (i18n "admin.fields.links.label" 1)
  "name" "links"
  "fields" $fields
  -}}

{{ partial "admin/widgets/list.html" $args }}
