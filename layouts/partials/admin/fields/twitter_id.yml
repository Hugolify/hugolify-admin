{{- $name := dict "name" "id" }}
{{- if (eq site.Params.admin.cms "tinacms") }}
  {{- $name = dict "name" "twitter_id" "nameOverride" "id" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.socials.twitter._id" | default "ID")
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/string.js" $args }}
