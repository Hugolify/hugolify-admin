{{- $options := slice "email" "checkbox" "radio" "select" "tel" "text" "textarea" -}}

{{- $args := dict 
  "label" (i18n "admin.fields.form.type.label")
  "name" "type"
  "default" "text"
  "required" true
  "options" $options
  -}}

{{ partial "admin/widgets/select.js" $args }}
