{{- $options := slice "ltr" "rtl" }}

{{- $args := dict 
  "label" (i18n "admin.fields.direction.label")
  "name" "direction"
  "options" $options
  -}}

{{ partial "admin/widgets/select.js" $args }}
