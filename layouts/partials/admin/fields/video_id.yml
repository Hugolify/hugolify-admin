{{- $name := dict "name" "id" }}
{{- if (eq site.Params.admin.cms "tinacms") }}
  {{- $name = dict "name" "video_id" "nameOverride" "id" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.video.id.label")
  "required" false
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/string.js" $args }}
