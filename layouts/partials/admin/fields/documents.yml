{{- $fields := slice 
  "title" 
  "pdf" -}}
{{- $fields = partial "admin/func/GetFields.html" (dict "field" . "fields" $fields) -}}

{{- $args := dict 
  "label" (i18n "admin.fields.document.label" 2)
  "label_singular" (i18n "admin.fields.document.label" 1)
  "name" "documents"
  "fields" $fields
  -}}

{{ partial "admin/widgets/list.html" $args }}