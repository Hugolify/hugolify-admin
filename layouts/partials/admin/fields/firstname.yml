{{/* A person is named the same in every language, the field is only filled once
  and copied to the other locales. */}}
{{- $args := dict
  "label" (i18n "admin.fields.firstname.label")
  "name" "firstname"
  "i18n" "duplicate"
  -}}

{{ partial "admin/widgets/string.js" $args }}
