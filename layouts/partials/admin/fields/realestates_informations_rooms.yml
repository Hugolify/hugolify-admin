{{- $name := dict "name" "rooms" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "realestates_informations_rooms" "nameOverride" "rooms" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.realestates_informations.rooms.label")
  "i18n" "duplicate"
  "min" 0
  "step" 1
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/number.js" $args }}

