{{- $name := dict "name" "webm" }}
{{- if or (eq site.Params.admin.cms "tinacms") (eq site.Params.admin.cms "cloudcannon") }}
  {{- $name = dict "name" "video_webm" "nameOverride" "webm" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.video.webm.label")
  "hint" (i18n "admin.fields.video.webm.hint")
  "extensions" "webm"
  "type" "video"
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/file.js" $args }}
