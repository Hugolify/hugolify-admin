{{- $image := dict "image" (dict "is_background" true "is_darken" true "has_mobile" true) -}}
{{- $video := dict "video" (dict "is_background" true "is_darken" true "has_mobile" true) -}}
{{- $uiFields := slice "align" "vertical_align" "is_darken" -}} 
{{- $ui := dict "ui" $uiFields -}}

{{- $fields := slice 
  "surtitle" 
  "title" 
  "text_markdown" 
  "ctas" 
  $image 
  $video 
  $ui -}}
{{- $fields = partial "admin/func/GetFields.html" (dict "field" . "fields" $fields) -}}

{{- $args := dict 
  "label" (i18n "admin.fields.hero.label")
  "name" "hero"
  "fields" $fields
  -}}

{{ partial "admin/widgets/object.js" $args }}
