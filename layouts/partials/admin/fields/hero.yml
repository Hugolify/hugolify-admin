{{- $fields := slice 
  "title" 
  "text" 
  "ctas" -}}
{
  label: '{{ i18n "admin.fields.hero.label" }}',
  name: 'hero',
  widget: 'object',
  required: false,
  i18n: true,
  {{ partial "admin/fields/_fields.yml" $fields }}
}