{{- $fields := slice 
  "diagnostic_epd" 
  "diagnostic_ghg" 
  "diagnostic_inprogress" -}}
{{- $fields = partial "admin/func/GetFields.html" (dict "field" . "fields" $fields) -}}

{{- $args := dict 
  "label" (i18n "admin.fields.diagnostic.label")
  "name" "diagnostic"
  "fields" $fields
  -}}

{{ partial "admin/widgets/object.yml" $args }}
