{{- $name := dict "name" "card" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "is_card" "nameOverride" "card" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.is_card.label")
  "hint" (i18n "admin.fields.is_card.hint")
  -}}

{{- $args = merge $args $name -}}

{{ partial "admin/widgets/boolean.js" $args }}
