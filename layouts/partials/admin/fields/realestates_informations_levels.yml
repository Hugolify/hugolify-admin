{{- $name := dict "name" "levels" }}
{{- if or (eq site.Params.admin.cms "tinacms") (eq site.Params.admin.cms "cloudcannon") }}
  {{- $name = dict "name" "realestates_informations_levels" "nameOverride" "levels" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.realestates_informations.levels.label")
  "i18n" "duplicate"
  "min" 0
  "step" 1
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/number.js" $args }}

