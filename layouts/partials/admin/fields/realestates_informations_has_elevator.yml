{{- $name := dict "name" "has_elevator" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "realestates_informations_has_elevator" "nameOverride" "has_elevator" }}
{{- end }}
  
{{- $args := dict 
  "label" (i18n "admin.fields.realestates_informations.has_elevator.label")
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/boolean.js" $args }}
