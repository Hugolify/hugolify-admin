{{- $options := partial "admin/func/GetOptions.html" (dict "field" . "options" .) -}}

{{- $args := dict
  "label" (i18n "admin.fields.grid.label")
  "hint" (i18n "admin.fields.grid.hint")
  "name" "grid"
  "required" false
  "options" $options
  -}}

{{ partial "admin/widgets/select.js" $args }}
