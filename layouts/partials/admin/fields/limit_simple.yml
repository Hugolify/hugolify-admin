{{- $name := dict "name" "limit" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "limit_simple" "nameOverride" "limit" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.limit.label")
  "hint" (i18n "admin.fields.limit.hint")
  "default" 5
  "min" 1
  "max" 10
  "step" 1
  "i18n" "duplicate"
  "range" true
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/number.yml" $args }}
