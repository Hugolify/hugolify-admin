{{- $name := dict "name" "type" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "chart_type" "nameOverride" "type" }}
{{- end }}

{{- $options := slice "bar" "pie" "line" "polarArea" "radar" }}

{{- $args := dict 
  "label" (i18n "admin.fields.chart.type.label")
  "default" "bar"
  "options" $options
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/select.js" $args }}
