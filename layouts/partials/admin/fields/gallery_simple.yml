{{- $name := dict "name" "gallery" }}
{{- if or (eq site.Params.admin.cms "tinacms") (eq site.Params.admin.cms "cloudcannon") }}
  {{- $name = dict "name" "gallery_simple" "nameOverride" "gallery" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.gallery.label")
  "multiple" true
  -}}

{{- $args = merge $args $name }} 

{{ partial "admin/widgets/image.js" $args }}
