{{- $name := dict "name" "data" }}
{{- if or (eq site.Params.admin.cms "tinacms") (eq site.Params.admin.cms "cloudcannon") }}
  {{- $name = dict "name" "chart_data" "nameOverride" "data" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.chart.data.label")
  "hint" (i18n "admin.fields.chart.data.hint")
  "language" "JSON"
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/code.js" $args }}
