{{- $name := dict "name" "year" }}
{{- if or (eq site.Params.admin.cms "tinacms") (eq site.Params.admin.cms "cloudcannon") }}
  {{- $name = dict "name" "realestates_informations_year" "nameOverride" "year" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.realestates_informations.year.label")
  "hint" "1930"
  "i18n" "duplicate"
  "min" 1800
  "max" 2100
  "step" 1
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/number.js" $args }}

