{{- $options := slice "danger" "dark" "info" "light" "primary" "secondary" "success" "warning" -}}

{{- $args := dict 
  "label" (i18n "admin.fields.state.label")
  "name" "state"
  "default" "light"
  "required" true
  "options" $options
  -}}

{{ partial "admin/widgets/select.js" $args }}
