{{- $options := partial "admin/func/GetStateOptions.html" . -}}

{{- $args := dict 
  "label" (i18n "admin.fields.state.label")
  "name" "state"
  "default" "light"
  "required" true
  "options" $options
  -}}

{{ partial "admin/widgets/select.js" $args }}
