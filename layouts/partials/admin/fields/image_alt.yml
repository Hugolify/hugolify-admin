{{- $name := dict "name" "alt" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "image_alt" "nameOverride" "alt" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.image.alt.label")
  "hint" (i18n "admin.fields.image.alt.hint")
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/string.js" $args }}
