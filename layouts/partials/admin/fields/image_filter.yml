{{- $name := dict "name" "filter" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "image_filter" "nameOverride" "filter" }}
{{- end }}

{{- $options := slice "Box" "Lanczos" "CatmullRom" "Linear" -}}
{{- $options = partial "admin/func/GetOptions.html" (dict "field" . "options" $options) -}}

{{- $args := dict 
  "label" (i18n "admin.fields.image.filter.label")
  "label_options" "admin.fields.image.filter.options"
  "hint" (i18n "admin.fields.image.filter.hint")
  "options" $options
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/select.js" $args }}
