{{- $name := dict "name" "pattern" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "form_pattern" "nameOverride" "pattern" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.form.pattern.label")
  "hint" (i18n "admin.fields.form.pattern.hint")
  -}}

{{- $args = merge $args $name -}}

{{ partial "admin/widgets/string.js" $args }}
