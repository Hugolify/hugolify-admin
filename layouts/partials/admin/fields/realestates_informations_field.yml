{{- $name := dict "name" "field" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "realestates_informations_field" "nameOverride" "field" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.realestates_informations.field.label")
  "hint" (i18n "admin.fields.realestates_informations.field.hint")
  "i18n" "duplicate"
  "min" 0
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/number.js" $args }}

