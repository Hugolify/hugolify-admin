{{- $options := partial "admin/func/GetOptions.html" (dict "field" . "options" .) -}}

{{- $args := dict
  "label" (i18n "admin.fields.theme.label")
  "name" "theme"
  "options" $options
  "i18n" "duplicate"
  -}}

{{ partial "admin/widgets/select.js" $args }}
