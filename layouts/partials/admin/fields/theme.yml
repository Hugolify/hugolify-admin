{{- $options := slice "white" "light" "dark" "black" "accent" -}}
{{- $options = partial "admin/func/GetOptions.html" (dict "field" . "options" $options) -}}

{{- $args := dict
  "label" (i18n "admin.fields.theme.label")
  "name" "theme"
  "options" $options
  -}}

{{ partial "admin/widgets/select.js" $args }}
