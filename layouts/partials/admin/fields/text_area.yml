{{- $name := dict "name" "text" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "text_area" "nameOverride" "text" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.text.label")
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/text.js" $args }}
