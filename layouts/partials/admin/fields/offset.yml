{{- $options := slice "start" "center" "end" }}

{{- $args := dict 
  "label" (i18n "admin.fields.offset.label")
  "name" "offset"
  "options" $options
  -}}

{{ partial "admin/widgets/select.yml" $args }}