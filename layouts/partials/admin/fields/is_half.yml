{{- $name := dict "name" "half" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "is_half" "nameOverride" "half" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.is_half.label")
  -}}

{{- $args = merge $args $name -}}

{{ partial "admin/widgets/boolean.js" $args }}
