{{- $options := slice "start" "center" "end" -}}
{{- $options = partial "admin/func/GetOptions.html" (dict "field" . "options" $options) -}}

{{- $args := dict
  "label" (i18n "admin.fields.align.label")
  "hint" (i18n "admin.fields.align.hint")
  "name" "align"
  "options" $options
  "i18n" "duplicate"
  -}}

{{ partial "admin/widgets/select.js" $args }}
