{{- $name := dict "name" "src" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "image_src" "nameOverride" "src" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.image.src.label")
  "hint" (i18n "admin.fields.image.src.hint")
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/image.js" $args }}