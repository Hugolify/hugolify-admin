{{- $name := dict "name" "src_mobile" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "image_src_mobile" "nameOverride" "src_mobile" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.image.src.mobile.label")
  "hint" (i18n "admin.fields.image.src.mobile.hint")
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/image.js" $args }}
