{{- $fields := slice 
  "price" 
  "discount" 
  "is_sold" 
  "text" -}}

{
  label: '{{ i18n "admin.fields.offer.label" }}',
  name: 'offer',
  widget: 'object',
  required: false,
  i18n: true,
  collapsed: false,
  {{ partial "admin/fields/_fields.yml" $fields }}
}
