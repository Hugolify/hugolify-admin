{{- $name := dict "name" "perPage" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "carousel_perpage" "nameOverride" "perPage" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.carousel.perpage.label")
  "hint" (i18n "admin.fields.carousel.perpage.hint")
  "min" 1
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/number.js" $args }}
