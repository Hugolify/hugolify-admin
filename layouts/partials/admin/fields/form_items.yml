{{- $name := dict "name" "items" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "form_items" "nameOverride" "items" }}
{{- end }}

{{- $fields := slice 
  "label" 
  "name" 
  "form_type" 
  "form_options" 
  "form_value" 
  "is_required" 
  "is_form_full" 
  "placeholder" 
  "form_autocomplete" -}}
{{- $fields = partial "admin/func/GetFields.html" (dict "field" . "fields" $fields) -}}

{{- $args := dict 
  "label" (i18n "admin.fields.items.label" 2)
  "label_singular" (i18n "admin.fields.items.label" 1)
  "fields" $fields
  -}}

{{- $args = merge $args $name }} 

{{ partial "admin/widgets/list.html" $args }}
