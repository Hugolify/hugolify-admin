{{- $fields := slice 
  "breakpoints" 
  "carousel_params" -}}
{{- $fields = partial "admin/func/GetFields.html" (dict "field" . "fields" $fields) -}}

{{- $args := dict 
  "label" (i18n "admin.fields.responsive.label")
  "name" "responsive"
  "fields" $fields
  -}}

{{ partial "admin/widgets/list.js" $args }}