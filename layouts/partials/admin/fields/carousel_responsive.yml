{{- $name := dict "name" "responsive" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "carousel_responsive" "nameOverride" "responsive" }}
{{- end }}

{{- $fields := slice 
  "breakpoints" 
  "carousel_params" -}}
{{- $fields = partial "admin/func/GetFields.html" (dict "field" . "fields" $fields) -}}

{{- $args := dict 
  "label" (i18n "admin.fields.carousel.responsive.label")
  "fields" $fields
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/list.js" $args }}