{{- $name := dict "name" "image" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "image_seo" "nameOverride" "image" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.image.seo.label")
  "hint" (i18n "admin.fields.image.seo.hint")
  "max_file_size" "500000"
  "media_folder" "/static/assets/images/seo"
  "public_folder" "/assets/images/seo"
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/image.js" $args }}