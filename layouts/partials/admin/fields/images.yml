{{ $image_filter := cond site.Params.admin.media.specific_filter "image_filter" false }}
{{- $fields := slice 
  "image_src" 
  $image_filter
  "image_alt" 
  "credit" 
  "legend"
  "is_half" -}}
{{- $fields = partial "admin/func/GetFields.html" (dict "field" . "fields" $fields) -}}

{{- $args := dict 
  "label" (i18n "admin.fields.images.label" 2)
  "label_singular" (i18n "admin.fields.images.label" 1)
  "name" "images"
  "fields" $fields
  -}}

{{ partial "admin/widgets/list.html" $args }}