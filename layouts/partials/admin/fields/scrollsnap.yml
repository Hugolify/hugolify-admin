{{- $options := slice "none" "sm" "md" "lg" "xl" "all" -}}
{{- $options = partial "admin/func/GetOptions.html" (dict "field" . "options" $options) -}}

{{- $args := dict
  "label" (i18n "admin.fields.scrollsnap.label")
  "hint" (i18n "admin.fields.scrollsnap.hint")
  "name" "scrollsnap"
  "options" $options
  -}}

{{ partial "admin/widgets/select.js" $args }}
