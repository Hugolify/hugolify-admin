{{- $name := dict "name" "padding" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "carousel_padding" "nameOverride" "padding" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.carousel.padding.label")
  "hint" (i18n "admin.fields.carousel.padding.hint")
  "min" 0
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/number.js" $args }}
