{{- $name := dict "name" "exclusive" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "is_exclusive" "nameOverride" "exclusive" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.is_exclusive.label")
  -}}

{{- $args = merge $args $name -}}

{{ partial "admin/widgets/boolean.js" $args }}
