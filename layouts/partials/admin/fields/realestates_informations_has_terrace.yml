{{- $name := dict "name" "has_terrace" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "realestates_informations_has_terrace" "nameOverride" "has_terrace" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.realestates_informations.has_terrace.label")
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/boolean.js" $args }}
