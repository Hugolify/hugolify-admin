{{- $name := dict "name" "screenshot" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "is_screenshot" "nameOverride" "screenshot" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.is_screenshot.label")
  "hint" (i18n "admin.fields.is_screenshot.hint")
  -}}

{{- $args = merge $args $name -}}

{{ partial "admin/widgets/boolean.js" $args }}
