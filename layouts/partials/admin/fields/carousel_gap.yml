{{- $name := dict "name" "gap" }}
{{- if or (eq site.Params.admin.cms "tinacms") (eq site.Params.admin.cms "cloudcannon") }}
  {{- $name = dict "name" "carousel_gap" "nameOverride" "gap" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.carousel.gap.label")
  "hint" (i18n "admin.fields.carousel.gap.hint")
  "min" 0
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/number.js" $args }}
