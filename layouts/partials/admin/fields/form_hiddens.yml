{{- $name := dict "name" "hiddens" }}
{{- if or (eq site.Params.admin.cms "tinacms") (eq site.Params.admin.cms "cloudcannon") }}
  {{- $name = dict "name" "form_hiddens" "nameOverride" "hiddens" }}
{{- end }}

{{- $fields := slice 
  "name" 
  "value" -}}
{{- $fields = partial "admin/func/GetFields.html" (dict "field" . "fields" $fields) -}}

{{- $args := dict 
  "label" (i18n "admin.fields.hiddens.label" 2)
  "label_singular" (i18n "admin.fields.hiddens.label" 1)
  "fields" $fields
  -}}

{{- $args = merge $args $name }} 

{{ partial "admin/widgets/list.js" $args }}