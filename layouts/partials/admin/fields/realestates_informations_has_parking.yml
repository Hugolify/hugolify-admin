{{- $name := dict "name" "has_parking" }}
{{- if or (eq site.Params.admin.cms "tinacms") (eq site.Params.admin.cms "cloudcannon") }}
  {{- $name = dict "name" "realestates_informations_has_parking" "nameOverride" "has_parking" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.realestates_informations.has_parking.label")
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/boolean.js" $args }}
