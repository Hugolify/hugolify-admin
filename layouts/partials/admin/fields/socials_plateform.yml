
{{- $name := dict "name" "plateform" }}
{{- if or (eq site.Params.admin.cms "tinacms") (eq site.Params.admin.cms "cloudcannon") }}
  {{- $name = dict "name" "socials_plateform" "nameOverride" "plateform" }}
{{- end }}

{{- $options := slice "facebook" 
  "github" 
  "google" 
  "instagram" 
  "linkedin" 
  "pinterest" 
  "threads" 
  "tiktok" 
  "twitch" 
  "twitter" 
  "vimeo" 
  "youtube" }}

{{- $args := dict 
  "label" (i18n "admin.fields.socials.plateform.label")
  "required" true
  "options" $options
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/select.js" $args }}
