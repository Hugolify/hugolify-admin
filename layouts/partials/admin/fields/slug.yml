{{/*  Not supported in _index.md  */}}
{
  label: '{{ i18n "admin.fields.slug.label" }}',
  hint: '{{ i18n "admin.fields.slug.hint" }}',
  name: 'slug',
  widget: 'string',
  required: false,
  i18n: true,
  pattern:
    [
      "^(|[a-z0-9-]+)$",
      "{{ i18n "admin.fields.slug.pattern" }}",
    ]
}