{{- $name := dict "name" "column" }}
{{- if eq site.Params.admin.cms "tinacms" }}
  {{- $name = dict "name" "column_simple" "nameOverride" "column" }}
{{- end }}

{{- $args := dict 
  "label" (i18n "admin.fields.column.label")
  "name" "column"
  "default" 1
  "min" 1
  "max" 2
  "step" 1
  "i18n" "duplicate"
  "range" true
  -}}

{{- $args = merge $args $name }}

{{ partial "admin/widgets/number.js" $args }}
