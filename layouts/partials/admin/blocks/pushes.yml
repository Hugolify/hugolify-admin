{{- $items := dict "items" (slice "title" "text" "cta" "offset" "is_card" "background_color" "color" "image") -}} 
{{- $fields := slice 
  "heading" 
  "column_simple" 
  $items 
  "background" -}}
{{- $fields = partial "admin/func/get_fields" (dict "block" . "fields" $fields) -}}
{
  label: '{{ i18n "admin.blocks.pushes.label" }}',
  name: 'pushes',
  widget: 'object',
  required: false,
  i18n: true,
  collapsed: false,
  summary: '{{`{{heading.title}}`}}',
  {{ partial "admin/fields/_fields.yml" $fields }}
}
