{{- $fields := partial "admin/blocks/fields/logos.html" . -}}

{{- $args := dict 
  "label" (i18n "admin.blocks.logos.label")
  "name" "logos"
  "collapsed" false
  "fields" $fields
  -}}

{{ partial "admin/widgets/object.js" $args }}
