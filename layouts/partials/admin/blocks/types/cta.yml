{{- $fields = partial "admin/block/fields/cta.html" . -}}

{{- $args := dict 
  "label" (i18n "admin.blocks.cta.label")
  "name" "cta"
  "collapsed" false
  "fields" $fields
  -}}

{{ partial "admin/widgets/object.js" $args }}
