{{- $fields := partial "admin/blocks/fields/quote.html" . -}}

{{- $args := dict 
  "label" (i18n "admin.blocks.quote.label")
  "name" "quote"
  "collapsed" false
  "fields" $fields
  -}}

{{ partial "admin/widgets/object.js" $args }}
