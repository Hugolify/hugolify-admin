{{- $fields = partial "admin/block/fields/comparison.html" . -}}

{{- $args := dict 
  "label" (i18n "admin.blocks.comparison.label")
  "name" "comparison"
  "collapsed" false
  "fields" $fields
  -}}

{{ partial "admin/widgets/object.js" $args }}
