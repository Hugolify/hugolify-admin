{{- $fields := partial "admin/blocks/fields/embed.html" . -}}

{{- $args := dict 
  "label" (i18n "admin.blocks.embed.label")
  "name" "embed"
  "collapsed" false
  "fields" $fields
  -}}

{{ partial "admin/widgets/object.js" $args }}
