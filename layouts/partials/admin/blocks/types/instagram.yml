{{- $fields := partial "admin/blocks/fields/instagram.html" . -}}

{{- $args := dict 
  "label" (i18n "admin.blocks.instagram.label")
  "name" "instagram"
  "collapsed" false
  "fields" $fields
  -}}

{{ partial "admin/widgets/object.js" $args }}
