{{- $fields = partial "admin/block/fields/latest.html" . -}}

{{- $args := dict 
  "label" (i18n "admin.blocks.latest.label")
  "name" "latest"
  "collapsed" false
  "fields" $fields
  -}}

{{ partial "admin/widgets/object.js" $args }}
