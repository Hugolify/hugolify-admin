{{- $fields = partial "admin/block/fields/video.html" . -}}

{{- $args := dict 
  "label" (i18n "admin.blocks.video.label")
  "name" "video"
  "collapsed" false
  "fields" $fields
  -}}

{{ partial "admin/widgets/object.js" $args }}
