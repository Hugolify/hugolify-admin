{{- $fields = partial "admin/block/fields/title.html" . -}}

{{- $args := dict 
  "label" (i18n "admin.blocks.title.label")
  "name" "title"
  "collapsed" false
  "fields" $fields
  -}}

{{ partial "admin/widgets/object.js" $args }}
