{{- $fields := slice 
  "direction" 
  "surtitle" 
  "title" 
  "text_markdown" 
  "notes" 
  "ctas" 
  "image" 
  "background" -}}

{{- $fields = partial "admin/func/GetFields.html" (dict "block" . "fields" $fields) -}}

{{- return $fields -}}
