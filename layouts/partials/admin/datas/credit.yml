{{- $fields := slice "text_markdown" -}}
{{- $fields = partial "admin/func/GetFields.html" (dict "file" "credit" "fields" $fields) -}}
{
  label: '{{ i18n "admin.files.credit" }}',
  name: 'credit',
  file: 'data/credit.yml',
  i18n: true,
  {{- partial "admin/fields/_fields.yml" $fields -}}
}