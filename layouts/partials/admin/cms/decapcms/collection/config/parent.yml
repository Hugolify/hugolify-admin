{{ $nested := (index site.Params.admin.collections .).nested.depth | default site.Params.admin.nested.depth }}
{{ if gt $nested 2 }}
meta: {
  path: {
    label: '{{ i18n "admin.fields.parent.label" }}',
    widget: string,
    index_file: _index
  }
}
{{ end }}