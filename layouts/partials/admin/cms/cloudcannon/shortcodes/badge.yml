{{- $stateOptions := partial "admin/func/GetStateOptions.html" . -}}
  hugolify_badge:
    template: hugo_shortcode_named_args
    inline: true
    preview:
      text: {{ i18n "admin.shortcodes.badge.label" | default "Badge" }}
    definitions:
      shortcode_name: badge
      named_args:
        - source_key: text
          editor_key: text
          type: string
        - source_key: state
          editor_key: state
          type: string
          optional: true
    _inputs:
      text:
        type: text
      state:
        type: select
        options:
          values:
            {{- range $stateOptions }}
            - {{ . }}
            {{- end }}
