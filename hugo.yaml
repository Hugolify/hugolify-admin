module:
  mounts:
    - source: content
      target: content
    - source: i18n
      target: i18n
    - source: layouts
      target: layouts
    - source: static
      target: static

outputFormats:
  admin_config:
    baseName: config
    isPlainText: true
    mediaType: application/yaml
    notAlternative: false
mediaTypes:
  application/yaml:
    suffixes:
    - yml

params:
  admin:
    cms: decapcms
    default_locale: en
    depth: 5
    locales: "[en]"
    media:
      max_file_size: 700000
    netlify:
      identity: true
    preview: false

    collections:
      - pages
      - posts